@model List<StudentEducation>
<h1>@ViewData["Title"]</h1>

<div class="row mt-3">
    <div class="col-md-12">
        <partial name="_Messages" />
    </div>

    <div class="col-md-12">
        <a asp-action="Create" asp-area="Student" asp-controller="Education" class="btn btn-primary">Add Education</a>
    </div>

    <div class="col-md-12">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th>Degree</th>
                    <th>Institute with Location</th>
                    <th>From-To</th>
                    <th>Percentage/GPA</th>
                    <th>Document</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var i in Model)
                {
                    <tr>
                        <td>@i.Degree</td>
                        <td>@i.Institute</td>
                        <td>@i.Year</td>
                        <td>@i.Percentage</td>
                        <td>
                            <a href="~/storage/student/education/@i.FileUrl" target="_blank">Document</a>
                        </td>
                        <td>
                            <a asp-action="Delete" asp-controller="Education" asp-route-id="@i.Id" class="text-danger">Remove</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
